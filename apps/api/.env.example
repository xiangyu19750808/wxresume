# --- Database ---
DB_URL=file:./dev.db

# --- Auth / JWT ---
# 生成方法：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_ME

# --- Feature Flags (mock 开关) ---
FLAG_ENABLE_WXPAY=false
FLAG_ENABLE_RENDER_WORKER=false
FLAG_ENABLE_RESUME_PARSE=false

# --- WeChat OAuth (留空=走 mock) ---
WECHAT_OAUTH_APPID=
WECHAT_OAUTH_SECRET=
WECHAT_OAUTH_REDIRECT_URL=http://localhost:8080/v1/auth/wx/callback

# --- WeChat Pay JSAPI (留空=走 mock) ---
WXPAY_MCHID=
WXPAY_APPID=
WXPAY_SERIAL_NO=
WXPAY_PRIVATE_KEY_PATH=
WXPAY_NOTIFY_URL=http://localhost:8080/v1/order/callback

# --- COS / 对象存储 (留空=生成本地 mock 链接) ---
COS_BUCKET=
COS_REGION=
COS_SECRET_ID=
COS_SECRET_KEY=
COS_PUBLIC_BASE=http://localhost:8080/mock/

# --- LLM / 提示词 (留空=走简单规则引擎) ---
LLM_PROVIDER=
LLM_API_KEY=
LLM_BASE_URL=
