export class HardRequirementsService {
  async checkRequirements(resumeText: string, jdText: string): Promise<any> {
    // Extract and compare hard requirements between JD and resume
    // Should return hardCheckResult structure (placeholder)
    return { passed: false, reasons: [] };
  }
}
