export class WarningsService {
  async generateWarnings(resumeText: string, jdText: string): Promise<any[]> {
    // Apply high-risk warning rules to resume and JD content
    // Should return warning list (placeholder)
    return [];
  }
}
