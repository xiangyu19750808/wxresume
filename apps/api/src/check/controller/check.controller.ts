export class CheckController {
  async handleCheck(req, res) {
    // Placeholder for handling POST /v1/check
    // Expected input: resumeText, jdText
    // Should return status and screening structure (to be implemented)
    res.json({ status: 'pending', data: null });
  }
}
