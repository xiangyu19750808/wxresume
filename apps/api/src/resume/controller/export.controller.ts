export class ExportController {
  async handleExport(req, res) {
    // Placeholder for POST /v1/resume/export
    // Should generate PDF and upload to COS
    res.json({ status: 'pending', fileId: null, url: null });
  }
}
